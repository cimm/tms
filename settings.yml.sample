default:
  database:
    connection: mysql://localhost/tms
  twitter:
    user:
    consumer_key:
    consumer_secret:
    access_token:
    access_token_secret:
  archiving:
    lifetime_in_months: 3
test:
  database:
    connection: connection_string
  twitter:
    user: user
    consumer_key: consumer_key
    consumer_secret: consumer_secret
    access_token: access_token
    access_token_secret: access_token_secret
  archiving:
    lifetime_in_months: 3
